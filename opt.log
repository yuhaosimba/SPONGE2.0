# build_fock 优化记录

这份文件用于记录 Build_Fock CPU 路径已经做过的优化、已经证伪的方向、
统一 benchmark 口径，以及当前最短路径判断。

目的只有一个：避免后面重复优化、重复回退。

## 1. 统一 benchmark 口径

所有性能判断都以同一条 benzene 第 2 轮 `Build_Fock` 为准。

- 体系：`benzene`
- 方法：`HF/def2-qzvp`
- 关注指标：SCF 第 2 轮 `Build_Fock`
- 命令：

```bash
tmpdir=$(mktemp -d)
cp -r benchmarks/comparison/tests/pyscf/statics/benzene/sponge/. "$tmpdir"
cd "$tmpdir"
/usr/bin/time -f 'PROCESS_ELAPSED=%E RSS=%MKB' \
  /home/obuntu/research/SPONGE-Direct-Fock/.pixi/envs/dev-cpu/bin/SPONGE \
  -mdin mdin.txt \
  -qc_model_chemistry HF/def2-qzvp \
  -qc_scf_print_iter 1 \
  -qc_scf_profile_build_fock 1 \
  -qc_scf_max_iter 2
```

### 正确性验证（必须通过）

性能优化之前，必须确认 SCF 能收敛到正确答案：

```bash
# 小体系回归（快速）
pixi run -e dev-cpu compile
cd /path/to/project
pixi run -e dev-cpu -- python3 -m pytest \
  'benchmarks/comparison/tests/pyscf/tests/comp_rhf.py::test_rhf[he_def2-qzvp]' -v

# benzene 收敛性测试（完整 SCF，6-31g 跑得快）
cd benchmarks/comparison/tests/pyscf/statics/benzene/sponge
SPONGE -mdin mdin.txt -qc_model_chemistry "HF/6-31g" \
  -qc_scf_max_iter 100 -qc_scf_print_iter 1
# 预期：SCF 收敛到 E ≈ -230.62 Ha（PySCF 参考值 -230.6237 Ha）
# 100 轮在 float32 精度下约收敛到 -230.622（误差 < 0.002 Ha）
```

## 2. e3004ce SCF 发散 bug 定位与修复

### 现象

benzene HF/6-31g（66 基函数）跑 SCF：
- **cbecc52（重构前）**：正确收敛到 E = -230.57 Ha ✓
- **e3004ce（重构后）**：DIIS 锁定在错误的 -204 Ha，不发散但不收敛 ✗

### 排查过程

1. PySCF 零密度初猜 cycle 1 也得到 E = -204.01 Ha →
   第一轮 Build_Fock 结果正确（direct SCF 没问题）
2. 逐个回退 e3004ce 修改的文件：
   - 回退 build_fock_cpu.hpp 的 JK 快速路径 → 仍然 bug
   - 回退 cart2sph 优化 → 仍然 bug
   - **回退 mix_converge.hpp → bug 消失，正确收敛到 -230.62 Ha**
3. 确认 bug 在 e3004ce 重写的 mix_converge.hpp 中

### 根因

e3004ce 的 mix_converge.hpp 新增了 `QC_Density_Diff_Accumulate_Kernel`
（使用 `atomicAdd` 对 double 累加）和 `QC_Mat_Dot_Accumulate_Kernel`
等诊断代码。这些 kernel 在 CPU OpenMP 并行环境下可能导致内存安全问题，
破坏了 SCF 密度矩阵的更新。

### 修复方案

- 保留 e3004ce 的 direct SCF 代码（build_fock.hpp、build_fock_cpu.hpp、
  build_fock_gpu.hpp 等）
- 使用 cbecc52 的 mix_converge.hpp（安全的密度混合逻辑）
- 在 mix_converge.hpp 末尾添加安全的 print_iter 功能：仅读取已有的
  d_scf_energy 和 d_delta_e，不新增 kernel

## 3. 当前保留基线

### 代码状态

基于 e3004ce 的 direct SCF 重构 + 修复后的 mix_converge.hpp。
CPU/GPU Build_Fock 已分离（build_fock_cpu.hpp / build_fock_gpu.hpp）。
DIIS 默认从 iter 1 开始（与 PySCF 一致）。

### 当前 benchmark 数字

benzene HF/def2-qzvp iter 2 Build_Fock：

- `Build_Fock total = ~27 s`
- `direct = ~27 s`
- `eri = ~594 s`（线程累计，24 线程）
- `jk = ~29 s`（线程累计）

### 已应用的优化

1. **sparse term list + pair-view 驱动累加**（3.3 核心）
   - QC_Angular_Term_CPU 结构体 + QC_Build_Angular_Terms_CPU
   - 预计算 hr_offset，替代 6 层嵌套循环
   - bra terms 每 bra primitive 构建一次，复用跨 ket primitives
   - 效果：152.8 s → 31.8 s

2. **低角动量快路径 + 4x unroll**（3.3 细化）
   - all l ≤ 1 走 stack-allocated term lists（最多 9 pairs × 27 terms）
   - generic path 内循环 4x unroll + outer/inner swap
   - 效果：31.8 s → 27 s

3. **Shell-level JK 快路径**（3.4）
   - I≠J, K≠L, IJ≠KL 的 general case 直接内联 8J+8K
   - jk 线程累计 157 s → 29 s

4. **CPU 专用 cart2sph**（3.5）
   - 预加载子矩阵 + float 累加 + broadcast-multiply 循环
   - cart2sph 降低（具体数字待独立测量）

## 4. 已证伪 / 已回退方向

### 4.1 更激进的 screening 参数

- 更大 qc_eri_pair_dist_screen_scale、更激进 prim_screen_tol
- wall time 反而更差
- screening 已不是最短路径

### 4.2 dense 半收缩中间量

思路：先把 bra terms 和 HR 收成 dense T[sx][sy][sz]

结果：cart 从 301 s 涨到 642 s。dense 中间量构造成本高于
sparse term list 节省。对中低 AM 的大量 quartets 尤其不利。

### 4.3 ket primitive / ket term 复用方案

- ket primitive cache / ket primitive 外层遍历
- 一直是负收益或接近无收益

### 4.4 pair-view 增加更多缓存字段 / descriptor / SoA

- benzene 第 2 轮大多数都变慢

### 4.5 bra-outer / ket-outer 双 helper / helper 拆分

- 编译器更难优化，内核形状变差

### 4.6 OpenMP 调度策略微调

- guided 等改法明显差于 dynamic

### 4.7 generic 热循环小 patch

- 更激进 unroll、小分支、restrict 提示等
- 大多数只是噪声或负收益

## 5. 正确性验证口径

### PySCF 参考值

| 体系 | 基组 | PySCF E(Ha) | SCF 轮数 | 初猜 |
|------|------|-------------|---------|------|
| benzene | 6-31g | -230.6237 | 8 | minao |
| benzene | 6-31g（零密度初猜）| -230.6237 | 12 | zeros |
| benzene | def2-qzvp | -230.7957 | 8 | minao |
| he | def2-qzvp | （回归测试） | — | — |

### SCF 收敛现状

- **benzene/6-31g**：DIIS start=2，~100 轮收敛到 -230.622 Ha ✓
  （float32 精度下收敛慢，最终精度 ~0.002 Ha）
- **benzene/def2-qzvp**：**无法收敛** ✗（当前首要目标）
  - 无 Kahan：DIIS 锁定在 -181 Ha，不改善
  - 有 Kahan（JK only）：iter 3 从 -181 跳到 -296，iter 4 发散
  - 有 Kahan（全路径）：iter 3 到 -312，iter 4 仍发散
  - PySCF float64 零密度初猜：cycle 1=-181, cycle 3=-217, 13 轮收敛
  - 说明 Kahan 在改善精度（iter 3 越来越好），但还不够
- **he/def2-qzvp**：回归测试通过 ✓

### DIIS 参数敏感性

- DIIS start=1：he/def2-qzvp 收敛到错误值 -2.75（应为 -2.86）
  → 第一轮密度太差导致 DIIS 锁定
- DIIS start=2：he 正确，benzene/6-31g 正确
- DIIS start=8：he 正确但需要更多轮
- DIIS stagnation reset：对已收敛体系误判为锁定，破坏结果。已删除。
- 当前默认：DIIS start=2

### 精度改善实验记录

Kahan 求和覆盖范围和 benzene/def2-qzvp iter 3 能量：

| 覆盖范围 | iter 3 E(Ha) | 说明 |
|----------|-------------|------|
| 无 Kahan | -181（锁定）| DIIS 不外推 |
| JK 快路径 only | -296 | 有外推但不够 |
| JK + ERI + fallback + reduce | -312 | 进一步改善 |
| PySCF float64 | -217 | 13 轮收敛到 -230.8 |

### 近似等 NAO 对照实验

目的：区分 "NAO 数量多" 和 "高角动量" 哪个更关键。

新增体系：`Ace-Ala-Ala-Ala-Ala-Nme`
- 方法：`RHF/def2-svp`
- 电子数：192
- **NAO = 485**（与 benzene/def2-qzvp 的 480 很接近）
- 最高角动量：`d`（没有 `f/g`）

构型来源：
- 用 `PeptideBuilder` 生成 `Ala4` 主链
- 手工补 `ACE/NME` 重原子
- 用 `OpenMM amber14-all.xml` 补氢并做快速最小化
- 静态 case 已加入 `benchmarks/comparison/tests/pyscf/statics/ace_ala4_nme`

PySCF 参考：
- `RHF/def2-svp`
- 初猜 `minao`
- `nao = 485`
- 前几轮：`init E=-1238.62`，`cycle 1 E=-1226.63`，随后向 `-1227.46` 收敛

SPONGE 手工观测（未额外做 profile，只开 `-qc_scf_print_iter 1`）：
- 命令：

```bash
cd benchmarks/comparison/tests/pyscf/outputs/rhf_ace_ala4_nme_hf_def2-svp_r1/sponge
SPONGE -mdin mdin.txt -qc_model_chemistry HF/def2-svp \
  -qc_scf_max_iter 6 -qc_scf_print_iter 1
```

- 前 6 轮能量：
  - iter 1: `+2197.90 Ha`
  - iter 2: `-594.72 Ha`
  - iter 3: `-872.38 Ha`
  - iter 4: `-958.03 Ha`
  - iter 5: `-995.91 Ha`
  - iter 6: `-1046.77 Ha`
- `Core Run Wall Time = 53.04 s`
  - 粗略平均：`~8.8 s / iter`
  - 注意：这是完整 SCF 前 6 轮总时间，**不是** `Build_Fock` profile

结论：
- 高角动量 `f/g` **不是必要条件**
- 仅仅把体系换成 **485 NAO 且最高只到 d**，SCF 仍然从 iter 1 就严重偏离 PySCF
- 当前问题更像是 **大 NAO 下 SCF 前段矩阵链路/状态更新** 有 bug，
  不能再把主因简单归结为 "高角动量太高" 或 "double 不够"

### 验证要求

1. he_def2-qzvp 回归测试通过
2. benzene/6-31g 能收敛到 E ≈ -230.6 Ha（误差 < 0.1 Ha）
3. benzene/def2-qzvp SCF 能收敛（**当前首要目标**）

## 6. 根因定位：基组系数归一化约定不一致

### 排查路径

1. 大 NAO 体系 SCF 发散 → 尝试了 double Fock 累加、dsyevd、
   double X 等各种混合精度改造，全部无效
2. dump iter 2 的 F、X、P 矩阵到文件，用 Python 对拍
3. 发现：**用 PySCF 的 X + SPONGE 的 F 做对角化，P max=216（正常）**
   **用 SPONGE 的 X + SPONGE 的 F，P max=16570（爆炸）**
4. 进一步发现 SPONGE X 有误差（X diff max=65 vs PySCF X）
5. 追溯到 overlap S：**SPONGE Cartesian S 和 PySCF Cartesian S
   差异 max=11.57**
6. 根因：**SPONGE 的 Cartesian S 对角线不是 1.0**
   - s shell: S_diag = 3.74 ~ 12.57（应为 1.0）
   - p shell: S_diag = 0.48 ~ 4.19（应为 1.0）

### 根因

**SPONGE 基组系数的归一化约定和 PySCF/libcint 不一致。**

SPONGE 的 `basis_def2_qzvp.hpp` 中存储的 contraction coefficients
包含了 primitive GTO normalization（`(2α/π)^{3/4}`）但：
- **缺少 `sqrt(4π/(2l+1))` 的球谐归一化因子**
- **contracted shells 缺少 contraction normalization**

具体比较（H 第一个 s shell, 4 primitives contracted）：
- SPONGE coeffs: `[0.0918, 0.171, 0.288, 0.430]`
- PySCF env coeffs: `[0.693, 1.289, 2.172, 3.243]`
- 比率恒定 = 7.544 = `sqrt(4π) × contraction_norm`

对 uncontracted shells（单 primitive），SPONGE 和 PySCF 系数完全一致。

### 修复方案（进行中）

直接修改基组 hpp 文件中的系数，改用 PySCF `_env` 约定（已包含
完整归一化）。这样不需要运行时修正。

已生成 def2-qzvp 的 PySCF 约定系数，但编译时遇到 float 字面量
格式问题（整数缺小数点），待修复后测试。

### 当前代码状态

- `quantum_chemistry.cpp`：已去掉运行时修正（不需要了）
- `basis_def2_qzvp.hpp`：需要替换为 PySCF 约定系数（进行中）
- `diag_density.hpp`：包含 double matmul + dsyevd + 诊断 dump
  （这些在修复基组后可以简化回 sgemm + ssyevd）
- `pre_scf.hpp`：包含 dsyevd overlap + double X 构建 + S_cart dump
  （修复基组后也可以简化）

## 7. debug 教训

### 7.1 不要轻易归因于 float32 精度

多次在遇到数值偏差时第一反应是"float32 精度不足"，但实际原因
都是代码逻辑或数据问题：

- benzene SCF 发散（偏差 20 Ha）→ 归因 float32 → 实际是
  mix_converge.hpp 的新 kernel 破坏了密度矩阵
- 收敛慢 → 归因 "float32 下 DIIS 锁定" → 实际是 diis_start=8
- 大基组 SCF 发散 → 归因 float32 累加精度 → 试了 Kahan、
  double Fock、dsyevd、double X → 全部无效
  → **实际是基组系数归一化约定不一致**

完整排查路径记录：
1. 尝试 Kahan 求和（JK 快路径 → 全路径）：iter 3 从 -181 改善到
   -312，但仍然发散。误以为是精度改善的迹象。
2. 尝试 double Fock 累加：反而变差（iter 3 从 -312 回退到 -77）。
   这本该提示精度不是根因。
3. 尝试 dsyevd（double 特征值分解）：无变化。
4. 尝试 double X 矩阵：无变化。
5. overlap_eig_floor 扫描：无变化。
6. 最终通过 **dump 中间矩阵 + Python 对拍** 定位到 S 矩阵差异。

教训：
- 如果多种精度改进都无效，停下来 **dump 对拍**，不要继续猜
- 对拍应该从最上游开始（S 矩阵），而不是从下游（P 矩阵）
- 系数归一化约定是一种"全局数据 bug"，不是精度问题

### 7.2 大重构要逐文件验证

e3004ce 一次性改了 17 个文件（+2117/-246 行），导致 bug 混在
大量改动中难以定位。正确做法：
- 每改一个文件就跑 benzene/6-31g 收敛测试
- 出问题立刻知道是哪个文件引入的

## 8. 性能现状

benzene/def2-qzvp iter 2 Build_Fock = ~27 s（之前最佳 ~22 s）。
等收敛问题解决后再继续 term-count 派发优化。

## 9. 维护规则

后面只要有新结果，就立刻更新这份文件，至少补三件事：
- benchmark 数字
- 具体改了什么
- 正确性验证是否通过

## 10. 2026-03-20 最新验证

### 10.1 对第 6 节根因判断的修正

第 6 节里“缺少 `sqrt(4π/(2l+1))` 的球谐归一化因子”这个表述不够准确。

最新结论是：
- **contracted shell normalization 确实缺失**
- 但对 `S_cart` 的直接对拍表明，问题不是简单的“整体改成 PySCF `_env` 就结束”
- SPONGE 当前 `one_e` 笛卡尔积分核对 `s/p` 壳层采用的 AO 约定，与
  PySCF `cart=True` 不同
- 要让 `S_cart` 和 PySCF `int1e_ovlp` 一一对应，需要同时满足：
  - 坐标使用 **Bohr**
  - `s/p` 壳层系数额外乘 `sqrt((2l+1)/(4π))`

也就是说，当前头文件不应直接存 PySCF `cart=True` 的 coefficient；
而应存**适配 SPONGE 现有 Cartesian overlap kernel 的 coefficient**。

### 10.2 已完成修复

新增脚本：
- `scripts/generate_qc_basis_header.py`

作用：
- 从 PySCF basis 数据生成 `def2-svp` / `def2-qzvp` 头文件
- 对 contracted shells 补上 contraction normalization
- 对 `s/p` 壳层补上与 SPONGE 当前笛卡尔积分核匹配的角归一化缩放
- 提供 `--check` 校验模式

已重生成：
- `SPONGE/quantum_chemistry/basis/basis_def2_svp.hpp`
- `SPONGE/quantum_chemistry/basis/basis_def2_qzvp.hpp`

并确认 `build-dev-cpu` 可以重新编译通过。

### 10.3 S_cart 与 PySCF 直接对拍结果

对拍对象：
- `benzene / def2-qzvp`
- PySCF: `mol = gto.M(..., cart=True)` + `int1e_ovlp`
- SPONGE: 正式 `build-dev-cpu/SPONGE` 路径导出的 `/tmp/sponge_S_cart.bin`

结果：
- shape: `642 x 642`
- `max_abs = 5.012287e-06`
- `rms = 1.965067e-07`
- `mean_abs = 4.813292e-08`

可以认为 **SPONGE 的 Cartesian S overlap 已经和 PySCF 一一对应到
float32 数值噪声量级**。

### 10.4 comp-pyscf 回归现状

RHF 分流回归结果：
- `comp_rhf.py -k 'not ace_ala4_nme and not benzene'`
- 结果：`14 passed, 2 deselected in 1.55s`

说明：
- 这次基组 / `S_cart` 修正**没有打坏已有小中型 RHF case**

但两个大 case 还不能宣称恢复：
- `benzene / HF / def2-qzvp`
- `ace_ala4_nme / HF / def2-svp`

当前观察：
- `benzene / HF / def2-qzvp` 单独回归时，运行数分钟仍未刷出第一条
  SCF 结果，已手动停止
- `ace_ala4_nme / HF / def2-svp` 在整套 RHF 回归中仍然是重 case，
  没有完成

因此当前状态应表述为：
- **积分定义问题（至少 S 矩阵）已修正**
- **小/中型 RHF regression 正常**
- **大体系 SCF 问题仍在，主矛盾已从积分定义下沉到 SCF 行为本身**

## 11. 2026-03-21 SCF 收敛继续排查

### 11.1 已证实：全面对齐 PySCF 默认约定是正确方向

之前 10.2 的修复用了 "s/p 壳层系数额外乘 `sqrt((2l+1)/(4π))`"
的方案，让 `S_cart` 对角线 = 1.0，但 c2s 矩阵用 identity。

这套方案在 two-electron（ERI）路径上不自洽。修复后统一改成：
- 基组系数：直接用 PySCF `_env`（primitive norm + contraction norm）
- 不做 s/p 额外缩放
- cart2sph：s=`1/sqrt(4π)`，p=`sqrt(3/(4π))*I`
- d/f/g 保持 PySCF 默认 `cart2sph(l)` 约定

这部分现在视为 **Confirmed**，不再回退。

### 11.2 已证实：对拍必须使用 SPONGE 实际坐标

之前长期误判 `S_sph` 差 0.87，原因不是积分实现，而是对拍时
PySCF 使用了不同的 benzene 坐标（旋转了 90 度）。

**规则：所有 S/F/P/JK 对拍必须用 SPONGE `coordinate.txt` 构造 PySCF mol。**

在使用 SPONGE 坐标后：
- `S_sph` pre-norm vs PySCF `int1e_ovlp`：`max diff = 3.8e-5`
- 说明 one-electron 的 `S/cart2sph` 主约定已经基本对齐

### 11.3 已证实：当前最早显著分叉点在 iter 1 的 Fp / P0 链

在同坐标、同体系下做闭合链对拍：
- `F1 (= H_core, P=0)`：diff ≈ `0.0049`，基本一致
- `S_sph`：diff ≈ `3.8e-5`
- `X`：和 PySCF 有可见差异，但不足以单独解释后续全部问题
- `P0`：SPONGE dump 和 PySCF 重建仍有差异（`max diff ≈ 0.16`）

因此，当前**不能**把根因直接下沉到 ERI/JK 或 DIIS。
最早需要继续闭环的链路是：

`F1 -> Fp1 = X^T F1 X -> eig -> C -> P0`

### 11.4 已证实：手写 double Fp 路径存在实现/布局问题

之前为修精度加入的手写 double matmul（`diag_density.hpp`）
确实存在严重的实现/布局问题：

- `Fp1_dump` 和 Python 用同一份 `X_sp`、`F1_sp` 重建的
  `X_sp^T @ F1_sp @ X_sp` 差到 `72`
- 这不是数值精度问题，而是实现/存储约定问题

这部分可以视为 **Confirmed bug**。

但要注意：
- 这并不自动等价于“当前所有 P0/SCF 问题都已被这个 bug 解释完”
- 它只说明：`diag_density.hpp` 中那条手写 double Fp 路径不可信

### 11.5 当前最佳判断（不是最终结论）

截至目前，最稳的状态表述应为：

**Confirmed**
1. 基组 / c2s 的主约定已统一到 PySCF 默认约定
2. 对拍必须使用 SPONGE 的实际坐标
3. `S_sph` pre-norm 已基本正确
4. `F1 (= H_core)` 基本正确
5. 手写 double Fp 路径存在严重 bug

**Current Best Hypothesis**
1. 当前最早显著分叉点在 `iter 1` 的 `Fp1 / P0`
2. `iter 2` 的 `JK` 误差是否是主根因，必须在 `P0` 完全对齐后再重新评估
3. 小 HOMO-LUMO gap 会放大误差，但这是放大机制，不是根因结论

**Invalidated**
1. “问题已经完全缩到 ERI float32 累加”
2. “问题已经完全缩到 DIIS 稳定性”
3. “double Fock/dgemm/dsyevd 已经从根本上解决主问题”

## 12. debug 教训（更新）

### 12.1 对拍时必须用相同坐标

SPONGE coordinate.txt 和 PySCF atom string 可能有不同的坐标
（旋转、平移）。对拍 S/F/P 时必须从 SPONGE 的 coordinate.txt
读取坐标构造 PySCF mol，否则会误判。

### 12.2 不要猜 layout，用 BLAS

手写 double matmul 容易搞混 row/col-major，导致 Fp diff = 72
这样的灾难性错误。用 cblas_dgemm(CblasRowMajor, ...) 可以
完全消除 layout 歧义。

### 12.3 闭合链对拍

对拍必须用 SPONGE dump 的实际中间量，不能用 PySCF 的 H_core
代替 SPONGE 的 H_core，也不能用“假设某一步应该相同”代替实际 dump。
如果链不闭合（用了错误的替代量），就无法正确定位分叉点。

### 12.4 P_new max 恒定不变是重要信号

`P_new max = 16687` 这种“长时间几乎不变”的现象本身是重要信号，
但不能过早把它解释成单一根因。后续排查表明，至少有两条因素曾被
混在一起：
- 对拍坐标不一致
- `diag_density.hpp` 手写 double Fp 路径存在 layout bug

因此，看到“某个异常量长期不变”时，先把对拍口径和实现路径闭合，再下结论。

## 13. 维护规则

后面只要有新结果，就立刻更新这份文件，至少补三件事：
- benchmark 数字
- 具体改了什么
- 正确性验证是否通过
